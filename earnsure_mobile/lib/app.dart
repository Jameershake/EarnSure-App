import 'package:flutter/material.dart';

class EarnSureApp extends StatelessWidget {
  const EarnSureApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink(); // Removed - moved to main.dart
  }
}
